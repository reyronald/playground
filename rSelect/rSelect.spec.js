const assert = require('assert');
const rSelect = require('./rSelect');

const testCases = [
  { input: [], expected: [] },
];

describe('Random Select', () => {
  testCases.forEach((testCase, key) => {
    it(`Case ${+key+1}`, () => {
      
    });
  });
});
